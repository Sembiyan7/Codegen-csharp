get:
  description: Returns an array of User objects.
  operationId: getUsers
  tags:
    - Libraries / Users
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../components/schemas/User.yaml'
