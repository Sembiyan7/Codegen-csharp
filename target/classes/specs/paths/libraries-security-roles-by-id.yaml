get:
  summary: Retrieve single security role by ID.
  description: Used to retrieve a single security role by ID.
  operationId: getRole
  tags:
    - Libraries / Security Role
  parameters:
    - $ref: '../components/parameters/SecurityRoleId.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/SecurityRole.yaml'



